# 测试报告 - 2026-03-24

## 1. 测试结论

截至 `2026-03-24 14:57:25 +08:00`，本轮验证结论如下：

- 系统冒烟测试通过。
  - 仓库根目录入口 `node scripts/system-smoke.mjs`
  - `31 / 31` 通过
- 前端 npm 冒烟入口通过。
  - `npm run smoke:system`
  - `31 / 31` 通过
- 后端自动化测试通过。
  - `cmd /c npm test`
  - `11 / 11` 通过
- 前端生产构建通过。
  - `npm.cmd run build`
  - `vue-tsc --noEmit` 通过
  - `vite build` 通过
- Playwright 入口仍未形成可执行用例。
  - `npm run test:e2e`
  - 返回 `Error: No tests found`

综合判断：

- 结果页统计、答卷管理、问卷回收站、上传校验等主链路当前可通过自动化和构建验证。
- 当前最大测试缺口仍然是浏览器级 E2E 回归，以及附件批量下载这一条尚未完成的业务链路。

## 2. 测试环境

- 测试日期：`2026-03-24`
- 项目目录：`d:\QQFile\maxform\maxformmaxform`
- 时区：`Asia/Hong_Kong` / `+08:00`
- 后端测试端口：`63102`
- 前端允许来源：`http://127.0.0.1:63000`
- 数据库：
  - Host：`127.0.0.1`
  - Port：`3309`
  - Database：`survey_system`
  - User：`root`

说明：

- 系统冒烟使用真实 MySQL 开发库，不是 mock 环境。
- 冒烟脚本会创建临时用户、部门、文件夹、问卷，并在结束后清理测试数据。

## 3. 执行命令与结果

### 3.1 仓库级系统冒烟

执行目录：

- `maxformmaxform/`

执行命令：

```bash
$env:DB_HOST='127.0.0.1'
$env:DB_PORT='3309'
$env:DB_USER='root'
$env:DB_PASSWORD='123456'
$env:DB_NAME='survey_system'
$env:FRONTEND_URL='http://127.0.0.1:63000'
$env:PORT='63102'
node scripts/system-smoke.mjs
```

执行结果：

- 通过
- runId：`20260324061018`
- `31 / 31` 通过

### 3.2 前端 npm 冒烟入口

执行目录：

- `maxformmaxform/frontend/`

执行命令：

```bash
cmd /c npm run smoke:system
```

执行结果：

- 通过
- runId：`20260324061046`
- `31 / 31` 通过

说明：

- `frontend/package.json` 暴露的系统冒烟入口可正确调用 `../scripts/system-smoke.mjs`。
- 前端目录级入口没有因工作目录差异失效。

### 3.3 后端自动化测试

执行目录：

- `maxformmaxform/backend/`

执行命令：

```bash
cmd /c npm test
```

执行结果：

- 通过
- `11 / 11` 通过

当前覆盖项：

1. 删除存在子部门的部门应被拒绝。
2. 删除部门时应清理成员 `dept_id`。
3. `POST /api/users/import` 应正确返回 `created / skipped / errors`。
4. 删除问卷应进入回收站，而不是直接物理删除。
5. 清空回收站时应联动删除答卷。
6. `GET /api/surveys/:id/results` 应返回题目级统计结果。
7. `POST /api/surveys/:id/uploads` 允许已发布问卷的公共上传。
8. 上传数量超过题目限制时应被拒绝。
9. 提交无效上传 token 时应被拒绝。
10. 提交超过 `maxFiles` 的上传答案时应被拒绝。
11. 上传文件绑定到错误题号时应被拒绝。

### 3.4 前端生产构建

执行目录：

- `maxformmaxform/frontend/`

执行命令：

```bash
npm.cmd run build
```

执行结果：

- 通过
- `vue-tsc --noEmit` 通过
- `vite build` 通过

补充说明：

- 当前构建仍存在 Vite 大 chunk 警告。
- 这属于打包优化问题，不阻塞本轮功能验证。

### 3.5 Playwright 入口检查

执行目录：

- `maxformmaxform/frontend/`

执行命令：

```bash
cmd /c npm run test:e2e
```

执行结果：

- 未通过
- 错误：`Error: No tests found`

结论：

- `Playwright` 依赖和 npm 入口存在。
- 仓库内当前仍无实际 E2E 用例文件，浏览器级自动化测试尚未落地。

## 4. 与本轮开发相关的增量结论

本轮新增开发主要集中在“结果页真实统计”和“答卷管理收口”，对应验证结论如下：

- 结果接口的题目级统计输出已由后端自动化测试覆盖。
- 结果页中的设备、浏览器、操作系统统计已接入真实后端返回结构。
- 结果页中的“查看答卷 / 导出数据”已改为真实链路。
- `AnswerManagementPage.vue` 的页面重构未破坏前端类型检查与生产构建。

当前尚未补齐的增量测试：

- 结果页跳转到答卷管理页的浏览器级交互回归。
- 答卷导出下载行为的前端 E2E 验证。
- 附件批量下载能力的接口与测试。

## 5. 系统冒烟覆盖范围

本轮 `31` 个系统检查项全部通过，主覆盖链路包括：

1. 健康检查
2. 管理员登录与身份读取
3. 普通用户注册与身份读取
4. 部门创建
5. 成员导入与读取
6. 父子文件夹创建与删除保护
7. 工作文件夹创建
8. 问卷创建、移动、发布、公开读取
9. 问卷提交答卷
10. 读取问卷结果
11. 读取消息与标记已读
12. 读取审计日志
13. 问卷进入回收站、恢复、清空
14. 删除文件夹
15. 删除部门并清理成员归属
16. 删除导入成员

链路判断：

- 认证链路：通过
- 组织管理链路：通过
- 文件夹链路：通过
- 问卷主链路：通过
- 消息与审计链路：通过
- 回收站链路：通过

## 6. 复测记录

今天第一次并行执行系统冒烟时，出现过一次失败：

- 错误：`connect ECONNREFUSED 127.0.0.1:3309`
- 当次结果：尚未开始业务检查，`results` 为空

后续排查结果：

- 本机 `3309` 端口存在 MySQL 监听。
- 使用最小连接脚本可以正常连接 `survey_system`。
- 独立重跑系统冒烟后恢复正常并 `31 / 31` 通过。

当前判断：

- 更像测试期间的瞬时环境连接异常。
- 暂未发现稳定复现的代码级问题。

## 7. 当前未覆盖项与风险说明

虽然本轮系统冒烟、后端自动化测试和前端构建均已通过，但以下范围仍未形成完整覆盖：

- 浏览器级 UI 自动化回归
  - 当前 `npm run test:e2e` 无可执行用例
- 结果页与答卷管理页的真实用户交互回归
  - 缺少前端自动化点击、跳转、下载覆盖
- 附件批量下载
  - 后端接口尚未实现
- 高级题型闭环
  - `rating / scale / matrix` 尚未形成创建、填答、提交、统计全链路
- 压测与稳定性验证
  - 尚未覆盖高并发提交、大批量导入、长时间运行稳定性

## 8. 建议的下一步测试

1. 为 `Playwright` 落地首批真实用例，至少覆盖登录、创建问卷、发布、填答、结果页、回收站。
2. 为结果页到答卷管理页的跳转与导出补浏览器级回归。
3. 为附件批量下载能力补接口测试和前端交互测试。
4. 为上传题补更完整的格式、大小、删除与异常路径验证。
5. 在前端拆包优化后补一轮构建与加载回归。
