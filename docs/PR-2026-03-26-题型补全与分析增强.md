# PR 摘要 - 2026-03-26 题型补全与分析增强

## 变更主题

本轮变更完成两件事：

1. 把 `matrix multi / matrix dropdown / ratio` 从旧文档里的“未交付”推进到真实闭环。
2. 把结果页从“部分真实统计 + 大量占位交互”推进到真实图表、时间筛选和下载动作。

## 主要改动

### 1. 共享题型模型收口

- 在 `shared/questionTypeRegistry.js` 中新增：
  - 服务端题型 `ratio`
  - 统一的 `submissionKind`
  - `21 / 24 / 36` 三类 legacy 题型实现定义
- 新增 `shared/questionModel.js`：
  - 统一后端结果接口与前端题目分析面板的统计口径
  - 支持 `ratio` 平均占比统计
  - 支持矩阵单选与矩阵多选统计

### 2. 后端提交流程与统计增强

- `backend/src/utils/questionSchema.js`
  - 提交校验改为按 `submissionKind` 分流
  - 支持矩阵多选答案结构校验
  - 支持比重题数值范围与总和 `100` 校验
- `backend/src/routes/surveys.js`
  - 复用共享题目统计模型
- `backend/test/routes.test.js`
  - 新增矩阵多选 / 矩阵下拉正向提交用例
  - 新增比重题正向提交用例
- `scripts/system-smoke.mjs`
  - 分析问卷新增比重题样本
  - 系统冒烟新增比重题统计断言

### 3. 前端编辑、填写、结果页同步

- `frontend/src/views/survey/CreateSurveyPage.vue`
  - 扩展矩阵与比重题配置进入真实创建/保存链路
- `frontend/src/views/survey/FillSurveyPage.vue`
  - 新增比重题作答 UI 与即时校验
  - 新增矩阵多选、矩阵下拉渲染分支
- `frontend/src/views/survey/SurveyAnswersPanel.vue`
  - 接入共享统计模型
  - 新增真实题目图表、时间筛选、附件下载和 SPSS 语法下载
- `frontend/src/views/survey/QuestionAnalyticsChart.vue`
  - 按题型渲染 choice / rating / metric / matrix / ratio / files 图表
- `frontend/src/views/survey/AnswerManagementPage.vue`
  - 补答卷时间筛选与筛选状态回显

### 4. 构建与交互细节

- `frontend/vite.config.ts`
  - 增加 `manualChunks`，细分 Vue、Element Plus、ECharts、Quill 等 vendor 包
- `frontend/src/components/QuillRichTextDialog.vue`
  - 改为按需加载 `highlight.js`
- `frontend/src/api/http.ts`
  - 支持下载接口失败时从 Blob 中解析 JSON 错误

## 验证结果

- 根目录系统冒烟：`56 / 56` 通过，runId `20260326070109`
- 前端 npm 冒烟入口：`56 / 56` 通过，runId `20260326070038`
- 后端自动化测试：`18 / 18` 通过
- 前端构建：通过
- `npm run test:e2e`：失败，`Error: No tests found`

## 影响判断

- 正向影响
  - 三类扩展题型已具备编辑、填写、提交、统计闭环
  - 题目统计模型前后端口径统一
  - 结果页真实可用性显著增强
- 残余风险
  - 浏览器级 E2E 仍缺
  - 地域统计正向数据仍依赖来源地落库
  - smoke 两个入口并行触发时存在 `runId` 秒级冲突风险

## 建议 follow-up

1. 先修复 smoke `runId` 并行冲突。
2. 再补结果页与下载链路的 Playwright 回归。
3. 最后评估 `31 / 32` 两类剩余评分族题型是否继续推进。
