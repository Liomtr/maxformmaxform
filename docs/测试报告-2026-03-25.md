# 测试报告 - 2026-03-25

## 1. 测试结论

截至 `2026-03-25`，本轮验证结论如下：

- 系统联调冒烟通过
  - 仓库级入口 `node scripts/system-smoke.mjs`
  - `55 / 55` 通过
  - runId: `20260325060823`
- 前端 npm 冒烟入口通过
  - `cmd /c npm run smoke:system`
  - `55 / 55` 通过
  - runId: `20260325060946`
- 后端自动化测试通过
  - `cmd /c npm test`
  - `15 / 15` 通过
- 前端生产构建通过
  - `npm.cmd run build`
  - `vue-tsc --noEmit` 通过
  - `vite build` 通过
- Playwright 入口仍未形成可执行用例
  - `cmd /c npm run test:e2e`
  - 返回 `Error: No tests found`

综合判断：

- 当前仓库可通过“系统冒烟 + 后端自动化测试 + 前端构建”完成阶段性联调验收。
- 本轮新增的职位管理、公开上传、上传题校验、答卷下载、附件打包、结果统计增强，已经纳入真实系统冒烟覆盖。
- 当前最大缺口仍是浏览器级 E2E 回归，而不是接口或构建阻塞。

## 2. 测试环境

- 测试日期：`2026-03-25`
- 项目目录：`d:\QQFile\maxform\maxformmaxform`
- 时区：`Asia/Hong_Kong` / `+08:00`
- 后端冒烟端口：`63102`
- 前端允许来源：`http://127.0.0.1:63000`
- 数据库：
  - Host：`127.0.0.1`
  - Port：`3309`
  - Database：`survey_system`
  - User：`root`

说明：

- 系统冒烟使用真实 MySQL 开发库，不是 mock 环境。
- 冒烟脚本会创建带时间戳的临时账号、部门、职位、文件夹、问卷、答卷和上传文件，并在流程尾部清理本轮测试数据。

## 3. 执行命令与结果

### 3.1 仓库级系统冒烟

执行目录：

- `maxformmaxform/`

执行命令：

```bash
$env:DB_HOST='127.0.0.1'
$env:DB_PORT='3309'
$env:DB_USER='root'
$env:DB_PASSWORD='123456'
$env:DB_NAME='survey_system'
$env:FRONTEND_URL='http://127.0.0.1:63000'
$env:PORT='63102'
node scripts/system-smoke.mjs
```

执行结果：

- 通过
- runId：`20260325060823`
- `55 / 55` 通过

### 3.2 前端 npm 冒烟入口

执行目录：

- `maxformmaxform/frontend/`

执行命令：

```bash
cmd /c npm run smoke:system
```

执行结果：

- 通过
- runId：`20260325060946`
- `55 / 55` 通过

说明：

- `frontend/package.json` 暴露的 npm 冒烟入口，已能稳定调用更新后的 `../scripts/system-smoke.mjs`。
- 前端目录级入口没有因工作目录差异而丢失数据库默认环境。

### 3.3 后端自动化测试

执行目录：

- `maxformmaxform/backend/`

执行命令：

```bash
cmd /c npm test
```

执行结果：

- 通过
- `15 / 15` 通过

当前自动化覆盖重点：

1. 删除存在子部门的部门应被拒绝。
2. 删除部门时应清理成员 `dept_id`。
3. `POST /api/users/import` 应正确返回 `created / skipped / errors`。
4. `GET /api/positions` 返回职位列表。
5. `POST /api/positions` 正确拦截重复编码。
6. 删除问卷进入回收站，而不是直接物理删除。
7. 清空回收站时联动删除答卷。
8. `GET /api/surveys/:id/results` 返回题目级统计。
9. `GET /api/surveys/:id/results` 聚合已有地域数据。
10. `POST /api/answers/download/attachments` 返回 zip 附件包。
11. `POST /api/surveys/:id/uploads` 允许已发布问卷公开上传。
12. 上传数量超过 `maxFiles` 时应被拒绝。
13. 上传题提交时使用错误 upload token 应被拒绝。
14. 上传题提交超过 `maxFiles` 应被拒绝。
15. 上传文件绑定到错误题号时应被拒绝。

### 3.4 前端生产构建

执行目录：

- `maxformmaxform/frontend/`

执行命令：

```bash
npm.cmd run build
```

执行结果：

- 通过
- `vue-tsc --noEmit` 通过
- `vite build` 通过

补充说明：

- 当前构建仍有 Vite 大 chunk 警告。
- 这属于打包优化问题，不阻塞本轮功能验收。

### 3.5 Playwright 入口检查

执行目录：

- `maxformmaxform/frontend/`

执行命令：

```bash
cmd /c npm run test:e2e
```

执行结果：

- 未通过
- 错误：`Error: No tests found`

结论：

- `Playwright` 依赖与 npm 入口仍在。
- 仓库内当前依旧没有实际 E2E 用例文件。

## 4. 本轮系统冒烟新增覆盖

本轮系统冒烟从旧口径 `31` 项扩展到 `55` 项，新增覆盖的主链路包括：

1. 职位管理链路：
   - 创建职位
   - 查询职位列表
   - 更新职位
   - 删除职位
2. 上传题公开上传链路：
   - 已发布问卷公开上传成功
   - 同题同会话超出 `maxFiles` 被拒绝
3. 上传题提交校验链路：
   - 错误 upload token 被拒绝
   - 文件绑定到错误题号被拒绝
4. 答卷管理链路：
   - 按问卷查询答卷列表
   - 导出问卷 Excel
   - 打包下载答卷附件 zip
5. 结果统计增强链路：
   - 汇总指标
   - 单选统计
   - 评分统计
   - 上传题统计
   - 设备 / 浏览器 / 操作系统统计
   - 地域空态返回结构

本轮 `55` 项系统检查全部通过，覆盖的大类能力如下：

1. 健康检查
2. 管理员登录与身份读取
3. 普通用户注册与身份读取
4. 部门创建、删除与成员归属清理
5. 职位创建、查询、更新、删除
6. 成员导入与读取
7. 父子文件夹创建与删除保护
8. 问卷创建、移动、发布、公开读取
9. 上传题公开上传与负向校验
10. 问卷答卷提交
11. 答卷列表、Excel 导出、附件包下载
12. 结果统计读取与题目级断言
13. 消息读取与已读标记
14. 审计日志读取
15. 回收站移动、查询、恢复、清空

## 5. 当前未覆盖项与风险说明

虽然本轮系统冒烟、后端自动化测试和前端构建均已通过，但以下范围仍未形成完整覆盖：

- 浏览器级 UI 自动化回归
  - 当前 `npm run test:e2e` 无可执行用例
- 结果页与答卷管理页的真实点击、跳转、下载行为
  - 接口链路已通过，浏览器交互仍缺自动化
- 地域统计正向链路的系统级验证
  - 后端单元测试已覆盖，有真实省市来源数据时仍建议补系统冒烟或 E2E
- 上传题提交流程的输入口径
  - 当前正向提交流程依赖在答案中携带 `name / type / size` 与 `id / uploadToken`
  - 服务端后续可考虑先解析 file id，再做类型校验，降低前端负担
- 打包体积优化
  - `vite build` 仍提示部分 chunk 体积较大

## 6. 建议的下一步

1. 为 `Playwright` 落地第一批真实用例，至少覆盖登录、创建问卷、公开填写、结果页、答卷管理页和回收站。
2. 为结果页到答卷管理页、Excel 导出、附件包下载补浏览器级回归。
3. 评估是否要收敛上传题提交契约，避免正向提交流程依赖额外文件元数据。
4. 在前端拆包优化后补一轮构建与加载回归。
